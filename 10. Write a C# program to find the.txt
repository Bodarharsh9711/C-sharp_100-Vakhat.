// 10. Write a C# program to find the largest of two numbers.

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
       
        int no1 = -1;
        int no2 = 2;
        int no3 = 5;
        if(no1>no2 && no1>no3)
            Console.WriteLine("the no.1 is bigger than no.2 and no.3");
        else if(no2>no1 && no2>no3)
            Console.WriteLine("the no.2 is bigger than no.1 and no.3");
        else
            Console.WriteLine("the no3 is bigger than no.1 and no.2");   

        
        Console.WriteLine("so, this is the hole process!");
        
    }   
}